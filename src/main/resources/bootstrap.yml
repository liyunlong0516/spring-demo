spring:
  application:
    name: spring-demo #如果同时有bootstrap.yml和application.yml，则建议把spring.application.name配置在bootstrap.yml文件里

  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848 #nacos配置中心地址
        file-extension: yml         #文件扩展格式
      discovery:
        server-addr: 127.0.0.1:8848 #nacos注册中心地址